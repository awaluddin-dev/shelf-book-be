generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider = "postgresql"
  schemas  = ["public", "user"]
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
  @@schema("user")
}

// ------------------------------------
// PORTFOLIO MODELS
// ------------------------------------

model PortfolioStatus {
  id     String @id @default("status_1") // Single row enforced
  status String @default("busy")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("portfolio_status")
  @@schema("public")
}

model HeroConfig {
  id           String @id @default("hero_1")
  name         String?
  role         String?
  availability String?
  openForWork  Boolean? @default(true)
  
  // New explicitly typed fields
  expertise    String?
  grit         String?
  service      String?
  
  config       Json?    // Kept for any future untyped fields

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("hero_config")
  @@schema("public")
}

model Metric {
  id        String   @id @default(uuid())
  value     String
  label     String
  icon      String?
  isSavings Boolean? @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("metrics")
  @@schema("public")
}

model Testimonial {
  id           String  @id @default(uuid())
  name         String
  role         String
  company      String
  relationship String
  testimonial  String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("testimonials")
  @@schema("public")
}

model WorkExperience {
  id          String @id @default(uuid())
  years       String
  duration    String
  company     String
  role        String
  stack       String
  teaser      String
  fullImpact  String
  bullets     Json

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("work_experiences")
  @@schema("public")
}

model CurrentFocus {
  id          String  @id @default(uuid())
  title       String
  icon        String
  description String
  link        String?
  linkText    String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("current_focus")
  @@schema("public")
}

model Proficiency {
  id     String             @id @default(uuid())
  title  String
  skills ProficiencySkill[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("proficiencies")
  @@schema("public")
}

model ProficiencySkill {
  id            String      @id @default(uuid())
  name          String
  subtext       String
  status        String
  proficiencyId String
  proficiency   Proficiency @relation(fields: [proficiencyId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("proficiency_skills")
  @@schema("public")
}

model Skill {
  id          String  @id @default(uuid())
  title       String
  category    String
  level       String
  details     String
  x           Int     @default(0)
  y           Int     @default(0)
  connections String[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt @default(now())

  @@map("skills")
  @@schema("public")
}

model Roadmap {
  id          String  @id @default(uuid())
  tech        String
  quarter     String
  status      String
  icon        String?
  description String
  depth       String?
  topics      String
  projects    String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("roadmaps")
  @@schema("public")
}

model Project {
  id          String   @id @default(uuid())
  title       String
  subtitle    String
  category    String
  date        String
  tags        String[]
  spineColor  String
  coverColor  String
  spineText   String
  github      String?
  demoUrl     String?
  stats       Json?
  phases      Json?
  markdown    String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("projects")
  @@schema("public")
}